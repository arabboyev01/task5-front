import React from 'react';
import { HeaderContainer } from '@/components/Header/style.header'

const Header: React.FC = () => {
  return (
    <HeaderContainer>
      <h1>Fake User Data Generator</h1>
    </HeaderContainer>
  );
};

export default Header;